                     🟦 SAMPLE EXTENSIONS  
        ┌────────────────────────────────────────────┐
        │ tiny-extension, test-extension-2, test-3   │
        └───────────────────┬────────────────────────┘
                            🟪
                            🟪 Static Scan Flow
                            🟪
          🟩 STATIC SCANNER (static_scanner.py)
        ┌─────────────────────────────────────────────┐
        │ - Reads manifest.json                       │
        │ - Checks permissions, host permissions      │
        │ - Detects suspicious code patterns          │
        └───────────────┬────────────────────────────┘
                        🟪
                        🟪 Batch Mode
                        🟪
              🟩 BATCH SCANNER (batch_scan.py)
        ┌────────────────────────────────────────────┐
        │ - Scans all sample extensions automatically│
        │ - Exports combined report                  │
        └───────────────┬───────────────────────────┘
                        🟪
                        🟥 STATIC OUTPUT
                        ▼
                scan_results.csv
──────────────────────────────────────────────────────────────────

                              🟪 Runtime Trigger
                              🟪
       🟧 CHROME (via Puppeteer - dynamic_runner.js)
        ┌─────────────────────────────────────────────┐
        │ - Launches browser                          │
        │ - Loads tiny-extension                      │
        │ - Opens example.com                         │
        │ - Triggers extension via keyboard shortcut  │
        └───────────────┬────────────────────────────┘
                        🟪
                        🟪 Extension Behaviour
                        🟪
                   🟧 TINY EXTENSION
        ┌────────────────────────────────────────────┐
        │ background.js                              │
        │ - OnInstalled → POST /collect              │
        │ - OnClicked → POST /collect                │
        └───────────────┬───────────────────────────┘
                        🟪
                        🟪 Sends Log Data
                        🟪
           🟩 FASTAPI BACKEND (http://127.0.0.1:8000)
        ┌────────────────────────────────────────────┐
        │ - Receives extension logs                  │
        │ - Prints/stores them                      │
        └───────────────┬───────────────────────────┘
                        🟪
                        🟥 DYNAMIC OUTPUT
                        ▼
          results/network_logs/log_<timestamp>.txt
